#include <stdio.h> 
int main(void)  
{ 
int num, a, b = 0, remainder; 
printf("Enter an integer: "); 
scanf("%d", &num); 
a = num; 
while (num != 0)  
{ 
} 
}  
remainder = num % 10; 
b = b * 10 + remainder; 
num = num / 10; 
if (a == b)  
{ 
printf("%d is a palindrome.\n", a); 
else  
{ 
} 
printf("%d is not a palindrome.\n", a); 
return 0; 
}
